/*const body = document.querySelector('body');
body.style.fontFamily = 'Arial, sans-serif';

const listSpan = document.querySelectorAll('span');
listSpan[0].innerHTML = 'Dan';
listSpan[1].innerHTML = 'Icecream';
listSpan[2].innerHTML = 'Sunnyvale';

const listItems = document.querySelectorAll('li');
for (var i = 0; i < listItems.length; i++) {
  listItems[i].className = 'listItem';
}

const newImg = document.createElement('img');
newImg.className = "listItem";
newImg.src = 'https://avatars2.githubusercontent.com/u/26369061?s=88&v=4';
body.appendChild(newImg);


const books = [
  {title: "Book1", author: 'guy1', alreadyRead: false},
  {title: "Book2", author: 'guy2', alreadyRead: true}
];

const list = document.createElement('ul');
for (var i=0; i<books.length; i++) {
  const book = books[i];
  const info = document.createElement('li');
  info.innerHTML = book.title + book.author;
  list.appendChild(info);
};

document.body.appendChild(list);
*/

/*
const button = document.getElementById('lib-button');

const makeMadLib = function(event) {
  const noun = document.getElementById('noun').value;
  const adjective = document.getElementById('adjective').value;
  const person = document.getElementById('person').value;
  alert(noun + adjective + person);
};

button.addEventListener('click', makeMadLib);

*/
/*
const catImg = document.getElementById('cat');
catImg.style.position = 'absolute';
catImg.style.left = '0px';
const catWalk = function () {
  const oldLeft = parseInt(cat.style.left);
  const newLeft = oldLeft + 10;
  catImg.style.left = newLeft + 'px';
  console.log('Hi')
}

window.setInterval(catWalk, 1000);*/
