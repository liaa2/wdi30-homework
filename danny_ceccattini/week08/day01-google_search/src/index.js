import React, { Component } from 'react'; // which is actually preact
import ReactDOM from 'react-dom'; // Also preact
import 'preact/devtools';

import App from './components/App';

ReactDOM.render(<App />, document.body);
