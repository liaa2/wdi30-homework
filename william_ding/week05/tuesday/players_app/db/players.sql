CREATE TABLE players (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT,
  age INTEGER,
  nation TEXT,
  team TEXT,
  overall INTEGER,
  pac INTEGER,
  sho INTEGER,
  pas INTEGER,
  dri INTEGER,
  def INTEGER,
  phy INTEGER
);
