<h2>All Teams</h2>
<div id="team-grid">
  <% @teams.each do |team| %>
  <div>
    <a href="/teams/<%= team["id"] %>">
      <img src="<%= team["logo"] %>" alt="">
      <p><%= team["name"] %></p>
    </a>
  </div>
  <% end %>
<div>
