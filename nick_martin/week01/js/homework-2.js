/*
var drEvil = function(x) {
  if x === 1000000;
    console.log(`${ x } dollars (pinky)`);
    else {
      console.log(`${ x } dollars`);
    }
}

drEvil(50);
*/

/*
let mixUp = function(word1, word2) {
}

let firstWordLetter = word1.charAt[0];
let secondWordLetter = word2.charAt[0];
let firstWordWithoutFirstLetter = word1.replace(firstWordLetter, "");
let secondWordWithoutFirstLetter = word2.replace(secondWordLetter, "");
console.log(`${secondWordLetter}${firstWordWithoutFirstLetter} ${firstWordLetter}${secondWordWithoutFirstLetter}`);

mixUp(drunk, public);
*/


/*
var fixStart = function(x) {
  var word =
var firstLetter === word.charAt[0];
  if firstLetter === word.charAt[1];
    x.replace(x.charAt[1], '*');
  else if firstLetter === word.charAt[2];
    x.replace(x.chatAt[2], '*');
  else if firstLetter === word.charAt[3];
    x.replace(x.charAt[3], '*');

}
*/

var verbing = function(x) {
  if verbing.length >= 3;
    var lastThree = id.substr(verbing.length -3);
      if lastThree === "ing"
        console.log(`${ x }ly`);
      else console.log(`${ x }ing`);
  else console.log(`${ x }`)
}
